Future<String?> saveFile({
  required String fileName,
  required String contents,
  required String mimeType,
}) async {
  throw UnsupportedError('Guardar archivos no esta soportado en esta plataforma');
}
