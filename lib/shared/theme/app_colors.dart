import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF6C63FF);
  static const primaryLight = Color(0xFF9C95FF);
  static const primaryDark = Color(0xFF3D35CC);

  // Accent
  static const accent = Color(0xFF00D4AA);
  static const accentLight = Color(0xFF4DFFDB);

  // Dark theme backgrounds
  static const backgroundDark = Color(0xFF0F0F1A);
  static const surfaceDark = Color(0xFF1A1A2E);
  static const cardDark = Color(0xFF252540);
  static const borderDark = Color(0xFF2E2E4A);

  // Light theme backgrounds
  static const backgroundLight = Color(0xFFF5F5FF);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const cardLight = Color(0xFFF0EFFF);

  // Text
  static const textPrimary = Color(0xFFF0F0FF);
  static const textSecondary = Color(0xFF9999BB);
  static const textMuted = Color(0xFF555577);

  // Semantic
  static const success = Color(0xFF00C896);
  static const warning = Color(0xFFFFB547);
  static const error = Color(0xFFFF4B6E);
  static const info = Color(0xFF4B9EFF);

  // Category colors
  static const categoryGroceries = Color(0xFF4CAF50);
  static const categoryDelivery = Color(0xFFFF9800);
  static const categoryRestaurants = Color(0xFFE91E63);
  static const categoryTransportation = Color(0xFF2196F3);
  static const categoryEntertainment = Color(0xFF9C27B0);
  static const categoryHealth = Color(0xFF00BCD4);
  static const categoryUtilities = Color(0xFF607D8B);
  static const categoryShopping = Color(0xFFFF5722);
  static const categoryFuel = Color(0xFF795548);
  static const categoryRent = Color(0xFF3F51B5);
  static const categorySubscriptions = Color(0xFF00ACC1);
  static const categoryEducation = Color(0xFF8BC34A);
  static const categoryPets = Color(0xFFFFC107);
  static const categoryOther = Color(0xFF9E9E9E);

  // Chart gradient stops
  static const gradientStart = Color(0xFF6C63FF);
  static const gradientEnd = Color(0xFF00D4AA);
}
