# Hallazgos de auditoría SIN VERIFICAR

Generado el 2026-08-30 por una auditoría automática con siete lentes distintos
sobre el código.

## Leelo con desconfianza

**Estos hallazgos NO están verificados.** La auditoría tenía una segunda fase
donde un escéptico intentaba refutar cada uno, y esa fase murió por límite de
cuota: de 44 agentes, 37 fallaron. Un solo hallazgo llegó a verificarse.

Así que esto es una **lista de sospechas**, no de bugs confirmados. Es esperable
que una parte sean falsos positivos: código que no corre, escenarios que la UI
no permite, o decisiones deliberadas que están en
[estado-y-decisiones.md](estado-y-decisiones.md).

**Antes de arreglar cualquiera de estos, abrí el archivo y confirmalo.**

Hay duplicados: varios lentes miraron el mismo archivo y encontraron lo mismo
con distinto nombre.

## Ya confirmados y arreglados

Dos se verificaron a mano y eran reales. Ya están arreglados, con test de
regresión:

- **`monthly_spending_summary.dart`** — `signature` tenía los `$` escapados y
  devolvía el texto literal `$name:$totalCents:$visits` para todos los
  comercios. Como alimenta los `props` de Equatable, dos meses con comercios
  distintos comparaban iguales y los insights no se regeneraban: justo lo
  contrario de para lo que existe esa clase. Es la trampa del peso escapado que
  ya estaba documentada, y **el grep que teníamos anotado no detectaba esta
  forma**. El grep quedó corregido.
- **`expenses_screen.dart`** — el centro del donut mostraba los centavos crudos
  (`$105600` en vez de `$ 1.056,00`), mientras el encabezado de la misma
  pantalla usaba `Money.format` correctamente.

## Temas que aparecieron en varios lentes: VERIFICADOS Y RESUELTOS

Los cuatro se verificaron a mano el 2026-08-30. **Los cuatro eran reales.**

1. **El borrado de cuenta no borraba todo.** Firestore no borra las
   subcolecciones al borrar el documento padre, así que la foto de cada ticket
   quedaba en `receipts/{id}/media/thumb` **para siempre**: sin el uid, las
   reglas impedían que nadie volviera a leerlas ni borrarlas. Tampoco se
   borraban los grupos. Además de la cuota, incumplía el derecho de supresión.
   **Arreglado**, cuidando el límite de 500 escrituras por batch (400 tickets
   con su foto son 800 escrituras). Los grupos compartidos no se borran a
   propósito: son datos de otras personas.
2. **`Either` descartado en la UI.** Confirmado en dos lugares que tocan plata:
   borrar un gasto de grupo, y editar el total de un ticket. Este último era el
   peor: invalidaba el provider, así que un fallo mostraba de nuevo el valor
   viejo y parecía que el cambio se guardó y se deshizo solo. **Arreglado.**
3. **La subida a un Firebase Storage que no existe.** El peor de los cuatro:
   cada guardado de ticket llamaba a Storage con **45 segundos de timeout**,
   contra un servicio que exige Blaze y no está activo. Garantizado que falla,
   en el camino crítico. **Arreglado** con `ENABLE_CLOUD_STORAGE`, apagado por
   defecto.
4. **Las reglas no validan que `shares` y `paidBy` sumen el total.** Real, pero
   **no se puede arreglar en las reglas**: el lenguaje no tiene forma de sumar
   los valores de un mapa, y guardar un total redundante no sirve porque el
   cliente malicioso mentiría también ahí. Necesitaría validación server-side,
   o sea Cloud Functions, o sea Blaze. Queda documentado en `firestore.rules`,
   validado en el cliente, y **la UI ahora marca los gastos descuadrados** en
   vez de sumarlos en silencio.

## El resto, por archivo


### `lib/features/expenses/presentation/providers/expense_provider.dart`

- **[alta]:48** Guardar un gasto manual reporta exito cuando la escritura fallo: se pierde el gasto sin aviso
  - Falla: El usuario abre 'Nuevo gasto', pone 1.250,00, toca Guardar. La escritura a Firestore falla (reglas rechazan, la sesion expiro y _userId tira AuthException, o cualquier excepcion que el repo convierte en Left(NetworkFailure)). addManual devuelve null porque ref.mounted ya es false, y null en el contrato de este metodo significa EXITO. La hoja hace Navigator.pop(context, true), el gasto nunca se escribio, y no aparece ningun mensaje de error. El gasto se pierde en silencio. Identico en delete(): borrar un gasto que fallo se reporta como borrado.
  - Propuesta: El guard de ref.mounted no puede decidir el valor de retorno. Calcular el mensaje primero y proteger solo el state:
  return result.fold(
    (failure) { if (ref.mounted) state = AsyncValue.error(failure.message, StackTrace.current); return failure.message; },
    (_) { if (ref.mounted) state = const AsyncValue.data(null); return null; },
  );
Aplicar lo mismo a delete() en la linea 64.
- **[media]:81** currentMonthSummary congela DateTime.now(): al cruzar el cambio de mes con la app abierta, el dashboard muestra los numeros del mes pasado bajo el encabezado del mes nuevo
  - Falla: El usuario deja la PWA abierta (pestaña de escritorio, o la app instalada en el celular en background) del 31 al 1. currentMonthSummary se calculo con el DateTime.now() del 31 y solo se recalcula si expensesStreamProvider emite; sin gastos nuevos, no emite nada. Al volver a la app el dia 1: _SpendingCard imprime el mes con AppDate.monthYear(DateTime.now()) en el build del widget, o sea 'febrero 2026', pero el numero grande abajo es el total de enero. Peor: budgetStatuses cruza los topes contra categoryTotalsCurrentMonth, que sigue siendo enero, asi que el usuario ve 'Te pasaste en Comida' el primer dia del mes nuevo, cuando el presupuesto en realidad se reinicio.
  - Propuesta: Sacar el reloj del provider y hacerlo explicito: un provider `currentMonth` que exponga DateTime(now.year, now.month) y que se invalide en AppLifecycleState.resumed (y con un Timer al proximo cambio de mes si la app queda abierta). currentMonthSummary lo watchea en vez de llamar a DateTime.now(), y el dashboard formatea el encabezado desde summary.month en vez de desde DateTime.now(), asi encabezado y numero no pueden discrepar.

### `lib/features/expenses/presentation/screens/expenses_screen.dart`

- **[alta]:125** El centro del donut de Gastos muestra los centavos como si fueran pesos: 100x el monto real
  - Falla: El usuario gasto $12.345,67 este mes. La tarjeta de arriba de la misma pantalla dice '$ 12.345,67' (usa Money.format), y el numero grande en el centro del donut, tres centimetros mas abajo, dice '$1234567'. Dos cifras distintas para lo mismo en la misma pantalla, y la del donut esta 100 veces inflada. El parametro total es int de CENTAVOS (viene de totalSpendingCurrentMonthProvider -> summary.totalCents) y toStringAsFixed(0) sobre un int devuelve el entero crudo, sin dividir por 100 ni aplicar separadores de miles.
  - Propuesta: Reemplazar por Money.format(total). Queda consistente con la cabecera, con las porciones del donut y con el resto de la app, y respeta el locale es_UY.
- **[alta]:125** La pantalla de Gastos muestra los centavos crudos como si fueran pesos en el centro del donut
  - Falla: `total` es el total del mes EN CENTAVOS (viene de `totalSpendingCurrentMonthProvider` -> `MonthlySpendingSummary.totalCents`). `toStringAsFixed(0)` sobre un int lo imprime tal cual, sin dividir por 100. Con un mes de $12.345,67 la misma pantalla muestra dos numeros distintos: el encabezado de la tarjeta dice `$ 12.345,67` (usa `Money.format(total)`, linea 80) y el centro del donut, tres centimetros mas abajo, dice `$1234567`. Verificado en Dart: `1234567.toStringAsFixed(0)` devuelve `"1234567"`. Es un error de 100x, en la pantalla principal de gastos, en el numero que la gente mira.
  - Propuesta: Usar el mismo formateador que el resto de la app: `Text(Money.format(total), ...)`. El `\$` hardcodeado ademas ignora `Money.symbolFor(Money.currencyCode)`, asi que `Money.format` arregla las dos cosas de una.

### `lib/features/groups/data/repositories/group_repository.dart`

- **[alta]:68** La query de gastos de grupo no filtra por memberIds y las reglas la rechazan entera: ningun gasto de grupo se puede leer
  - Falla: Usuario A crea un grupo y carga un gasto (la escritura pasa, porque el create se evalua por documento contra esDelGrupo()). Al entrar a GroupDetailScreen, groupExpensesProvider llama a watchExpenses(groupId), que hace _expenses(groupId).snapshots() SIN ningun where. La regla de firestore.rules:139-140 para list es `request.auth.uid in resource.data.memberIds`, que en Firestore obliga a que la query traiga el filtro equivalente `where('memberIds', arrayContains: uid)`. Como no esta, la query entera falla con PERMISSION_DENIED (las reglas no filtran, rechazan). Resultado: la pantalla muestra siempre 'No se pudieron cargar los gastos', y groupDebts/myBalance (group_provider.dart:29 y :38) hacen `.value ?? const []`, o sea calculan balances sobre una lista vacia y devuelven cero deudas. La fase 1 de grupos, desplegada, no puede mostrar ni un gasto ni un balance. El propio repositorio documenta la regla que se olvido de aplicar aca: el comentario de watchGroups (lineas 51-55) dice 'una query sin este filtro es rechazada entera, no filtrada'.
  - Propuesta: Agregar el filtro que la regla espera: `_expenses(groupId).where('memberIds', arrayContains: _user.uid).snapshots()`. Es el mismo patron que ya usa watchGroups en la linea 57. Ojo tambien con el `.value ?? const []` de group_provider.dart:29 y :38: convierte un error de permisos en 'no debe nada', que es exactamente la falla silenciosa peor que un error visible.

### `lib/features/receipts/data/repositories/receipt_repository_impl.dart`

- **[alta]:278** Borrar un ticket sin miniatura falla entero: el batch borra receipts/{id}/media/thumb aunque no exista y la regla lo deniega
  - Falla: saveDraft solo escribe media/thumb `if (thumbnail != null)` (linea 201). buildBase64 devuelve null cuando img.decodeImage no puede decodificar la foto, caso concreto y frecuente en el camino que importa (PWA en iPhone: el picker de Safari entrega HEIC y package:image no lo decodifica). Ese ticket queda guardado y sin subdocumento thumb, que es justo el diseno declarado ('que falle nunca debe impedir guardar el ticket'). Despues, deleteReceipt arma el batch y siempre hace batch.delete(_thumbDoc(id)) sin chequear si existe. La regla de firestore.rules:76 es `allow delete: if owns()`, y owns() lee `resource.data.userId`: sobre un documento inexistente `resource` es null, el acceso a resource.data corta la evaluacion y el delete se deniega. Como un batch de Firestore es atomico, cae TODO el commit: el ticket no se borra, y el gasto asociado tampoco. El usuario ve un error generico y ese ticket queda imposible de borrar para siempre, sumando en el dashboard y en el presupuesto. Lo mismo pasa con cualquier ticket guardado antes de que existiera la miniatura.
  - Propuesta: Dos arreglos, conviene hacer los dos. En la regla: `allow delete: if resource == null || owns();` (firestore.rules:76), que es el patron estandar para no romper el borrado de documentos inexistentes. En el codigo: chequear `(await _thumbDoc(id).get()).exists` antes de agregar el delete al batch, o borrar la miniatura fuera del batch en un try/catch, igual que ya se hace con la imagen de Storage.
- **[alta]:160** Cada guardado en web espera hasta 45 s por una subida a Firebase Storage que nunca puede funcionar, antes de escribir en Firestore
  - Falla: Storage esta sin activar a proposito (plan Spark, documentado en estado-y-decisiones.md), asi que el bucket mborba-proyect.firebasestorage.app no existe. Igual, en cada 'Guardar' del escaneo se leen los bytes completos de la foto y se hace putData contra ese bucket. En el navegador la respuesta de un bucket inexistente no trae headers CORS, asi que el SDK de JS no ve un 404 sino un error de red, y los errores de red son reintentables: reintenta con backoff hasta maxUploadRetryTime (10 min por defecto), o sea hasta que corta el timeout propio de 45 s. Recien despues se arma la miniatura y se hace batch.commit(). Resultado concreto: la persona toca Guardar, el boton queda con el spinner casi un minuto sin ningun mensaje, y si cierra la pestaña o toca atras (que es lo razonable, parece colgado) el ticket y el gasto NO se escribieron nunca: se pierde el escaneo entero. Ademas se mandan varios MB de datos moviles por reintento, para nada.
  - Propuesta: Sacar la subida del camino de guardado mientras Storage este sin activar: dejar imageUrl = '' directamente (que es lo que ya termina pasando siempre) o ponerla detras de un flag de Environment. Si se quiere conservar el codigo para el dia que exista el bucket, bajar el timeout a 5 s y, sobre todo, invertir el orden: armar la miniatura y hacer batch.commit() PRIMERO, y recien despues intentar la subida y actualizar imageUrl con un update. El dato del ticket nunca debe depender de una request que puede colgarse.
- **[media]:211** Guardar un ticket o un gasto sin conexion deja la pantalla colgada para siempre, sin exito ni error
  - Falla: Usuario en el super, con el celular sin señal o con el WiFi cortado, escanea el ticket, revisa y toca Confirmar. `ScanNotifier.confirm` pone el estado en `ScanSaving` y espera `saveDraft`, que espera `batch.commit()`. El Future de `commit()` en Firestore solo se completa cuando el servidor confirma la escritura: sin red no se completa ni se rechaza, se queda pendiente indefinidamente. Como no hay ningun `.timeout()` sobre el commit (el unico timeout del metodo es el de los 45 s de la subida a Storage), el estado nunca sale de `ScanSaving` y la pantalla muestra el spinner para siempre. La persona no tiene forma de saber si el gasto quedo o no; lo mas probable es que recargue la PWA, y en web (sin persistencia offline habilitada en main.dart) la escritura en cola se pierde con la pestaña. Mismo camino en `addManualExpense` con `await _col.add(...)`.
  - Propuesta: Envolver el commit en un timeout corto y devolver un Failure explicito: `await batch.commit().timeout(const Duration(seconds: 15))`, y en el `on TimeoutException` devolver algo como 'Sin conexion: no se pudo confirmar el guardado'. Si se quiere que igual llegue cuando vuelva la red, dejar la escritura encolada (no se cancela) y avisar 'se guardara cuando vuelva la conexion', pero solo despues de habilitar persistencia en web (`FirebaseFirestore.instance.settings = Settings(persistenceEnabled: true)` en main.dart), porque hoy en web la cola muere con la pestaña.
- **[media]:201** Si falla la generacion de la miniatura, el ticket se guarda sin ninguna foto y la app dice que salio todo bien
  - Falla: La miniatura en `receipts/{id}/media/thumb` es la UNICA copia de la foto del ticket (Storage no esta activo, `imageUrl` queda en ''). `ThumbnailService.buildBase64` devuelve null y se traga la causa en tres casos: formato que `package:img` no decodifica (HEIC de iPhone elegido desde la galeria, algunos PNG raros), excepcion al decodificar una foto muy grande, o base64 que sigue arriba de 600 KB despues de los tres escalones. En `saveDraft` ese null se ignora sin log y sin cambiar el resultado: el batch se escribe igual y la UI muestra 'guardado'. La persona cree que la foto de la compra quedo respaldada; cuando abre el detalle mas tarde no hay imagen y ya no tiene el archivo original. En un ticket que pensaba usar como comprobante, esto es perdida de datos que se descubre tarde.
  - Propuesta: Que `buildBase64` distinga los tres casos (o al menos loguee el motivo) y que `saveDraft` no lo ignore: si `thumbnail == null`, guardar igual el ticket (el dato importa mas) pero devolver la señal a la UI para mostrar 'el ticket se guardo, pero no se pudo guardar la foto' en vez de un exito liso. Agregar un `_log.w` con el tamaño y el mime de la entrada para poder diagnosticarlo.
- **[media]:159** Cada guardado intenta subir la foto entera a un Storage que no existe, y espera hasta 45 s antes de rendirse
  - Falla: Firebase Storage no esta activado (plan Spark, documentado en estado-y-decisiones.md). Aun asi, cada vez que se confirma un ticket, `saveDraft` sube `uploadBytes` a `receipt_images/{uid}/...`. En web, que es el unico camino que usa esta persona, `uploadBytes` son los bytes CRUDOS del picker (no hay compresion, `flutter_image_compress` no soporta web): la foto de una camara de celular, tipicamente 3 a 8 MB. En un celular con datos moviles lentos eso significa transmitir varios MB del plan de datos para recibir un error, y hasta 45 segundos de spinner en 'Guardando' antes de que el timeout lo suelte y recien ahi se escriba el batch. Ademas el timeout no cancela la subida en curso, solo deja de esperarla.
  - Propuesta: Saltear la subida mientras Storage no este activo: un flag en `app.env` (por ejemplo `STORAGE_ENABLED=false`) leido desde Environment, y si esta en false no llamar a `uploadReceiptImageBytes` y dejar `imageUrl` en ''. Si se prefiere no meter flag, bajar el timeout a 5 s y solo intentar la subida cuando `!kIsWeb` (donde los bytes ya vienen comprimidos).
- **[media]:278** Borrar un ticket que no llego a tener miniatura falla entero: el batch borra un documento inexistente
  - Falla: `saveDraft` escribe `receipts/{id}/media/thumb` SOLO `if (thumbnail != null)`. `ThumbnailService.buildBase64` devuelve null cuando `img.decodeImage` no reconoce el formato (HEIC, un blob raro que el canvas del picker no reencodeo) o cuando los tres escalones pasan los 600 KB. Ese ticket queda guardado sin subcoleccion media.

Despues, `deleteReceipt` mete `batch.delete(_thumbDoc(id))` sin condicion. La regla de esa ruta es `allow delete: if owns();`, y `owns()` hace `request.auth.uid == resource.data.userId`. Para un documento que no existe `resource` es null, desreferenciar `resource.data` hace fallar la evaluacion y la regla deniega. Como es un batch, la denegacion tira TODO: no se borra el ticket, no se borran los gastos asociados, y el usuario ve un StorageFailure opaco. El ticket queda imposible de borrar desde la app, y su gasto sigue sumando en el dashboard y contra el presupuesto para siempre.

Mismo problema para cualquier ticket guardado antes de que existiera la subcoleccion media.
  - Propuesta: Dos arreglos, conviene hacer los dos. En el cliente: `final thumb = await _thumbDoc(id).get(); if (thumb.exists) batch.delete(_thumbDoc(id));` (una lectura mas, se paga solo al borrar). En las reglas, tolerar el documento ausente: `allow delete: if signedIn() && (resource == null || owns());`. Borrar algo que no existe es inocuo y no abre nada. Este es exactamente el tipo de cosa que el gatillo de 'tests de reglas contra el emulador' esta para agarrar.
- **[baja]:337** La lista de tickets esta clavada en 20 y no hay paginacion: el ticket 21 y su foto quedan inalcanzables
  - Falla: Usuario que escanea tres tickets por dia llega a 20 en una semana. `watchReceipts` pide siempre `.limit(AppConstants.receiptsPageSize)` (20) ordenado por `createdAt` descendente, y `ReceiptListScreen` renderiza `receipts.length` en un ListView sin scroll infinito, sin boton de 'ver mas' y sin ningun otro consumidor de `getReceipts({limit})`, que existe pero no lo llama nadie. A partir del ticket 21, los tickets anteriores no se pueden abrir desde ninguna pantalla, y con ellos se vuelve inalcanzable su foto, que solo se lee desde el detalle (`receiptThumbnailProvider`). Los documentos siguen en Firestore ocupando cuota, pero la app no ofrece camino para llegar a ellos salvo escribir la URL /receipts/{id} a mano, y el id es un uuid.
  - Propuesta: Agregar paginacion real: mantener el stream de 20 para el camino caliente y sumar un `loadMore` que use `getReceipts` con `startAfterDocument` sobre `createdAt`, acumulando en un notifier. Alternativa minima si no se quiere paginar: filtro por mes con `where('createdAt', isGreaterThanOrEqualTo: ...)`, que reusa el indice compuesto que ya esta declarado.
- **[baja]:198** El gasto que nace de un ticket se guarda a medianoche, no a mediodia como el manual: puede caer en el mes anterior
  - Falla: `draft.receiptDate` sale del parser como una fecha a las 00:00 hora local. `saveDraft` la guarda tal cual como Timestamp. Un ticket del 1 de setiembre guardado en Montevideo (UTC-3) queda como 2026-09-01T03:00Z. Si esa misma persona abre la PWA desde una computadora configurada en una zona al oeste (por ejemplo UTC-5, viajando o con la maquina mal configurada), `toDate()` lo devuelve como 31 de agosto 22:00 y `MonthlySpendingSummary` lo cuenta en agosto: el total del mes cambia y una alerta de presupuesto puede pasar de verde a excedida (o al reves) sin que se haya cargado ningun gasto. El camino manual ya se protege de esto normalizando a mediodia, con el comentario explicito; el camino del escaneo, que es el principal, no.
  - Propuesta: Normalizar igual que el manual en los dos campos que escribe el batch: `final d = draft.receiptDate ?? DateTime.now(); final fecha = DateTime(d.year, d.month, d.day, 12);` y usar `Timestamp.fromDate(fecha)` tanto en el gasto como en `receiptDate` del ticket. Lo mismo en `updateReceipt`, que hoy escribe `Timestamp.fromDate(receiptDate)` crudo.

### `lib/features/receipts/presentation/providers/receipt_provider.dart`

- **[alta]:97** Borrar un ticket siempre muestra "No se pudo borrar" aunque el borrado haya funcionado
  - Falla: El usuario abre el detalle de un ticket, toca Borrar y confirma. El repositorio hace get() del ticket, borra la imagen, consulta los gastos vinculados y hace batch.commit(): el ticket Y su gasto se borran de verdad. Pero delete() devuelve false y la pantalla muestra el snackbar rojo 'No se pudo borrar el ticket' y NO navega a /receipts. El usuario cree que falló, vuelve atras y el ticket ya no esta en la lista, o toca Borrar de nuevo y vuelve a ver el mismo error. Pasa SIEMPRE, no es una carrera rara.
  - Propuesta: Separar 'guardar estado' de 'devolver resultado'. Calcular el retorno del fold y solo proteger la escritura de state:
  final result = await ref.read(receiptRepositoryProvider).deleteReceipt(id);
  return result.fold(
    (f) { if (ref.mounted) state = AsyncValue.error(f.message, StackTrace.current); return false; },
    (_) { if (ref.mounted) state = const AsyncValue.data(null); return true; },
  );
Y ademas, para que el estado del notifier sirva de algo, o la pantalla hace ref.watch(receiptActionsProvider) (asi deja de auto-disposearse), o el notifier llama ref.keepAlive() en build(). Lo mismo aplica a todos los *Actions del proyecto.
- **[media]:78** Navegar mientras se guarda un ticket pierde la confirmacion: el ticket queda guardado pero la UI no lo dice, y el usuario lo escanea de nuevo
  - Falla: La barra de navegacion inferior sigue visible durante la revision del ticket. El usuario toca Guardar y saveDraft() arranca: primero intenta subir la foto a Firebase Storage con .timeout(45 segundos), y Storage esta SIN ACTIVAR en el proyecto (documentado en estado-y-decisiones.md), asi que ese intento tarda hasta 45 segundos antes de caer al catch. Con el spinner ahi parado, el usuario toca la pestaña 'Gastos'. ScanReceiptScreen se desmonta, scanProvider se auto-disposea, y cuando el batch.commit() termina, `if (!ref.mounted) return` impide pasar a ScanSaved: nunca sale el snackbar 'Ticket guardado' ni el go('/receipts'). El ticket y su gasto SI se escribieron. El usuario vuelve a /scan, ve la pantalla vacia en ScanIdle, y vuelve a escanear el mismo ticket: queda duplicado, y el gasto se cuenta dos veces en el total del mes y en el presupuesto de la categoria.
  - Propuesta: Dos cosas. (a) Que confirm() no dependa de que la pantalla siga viva: llamar ref.keepAlive() al empezar el guardado y cerrar el link cuando la operacion termina, asi ScanSaved sobrevive a la navegacion y al volver a /scan se ve el resultado. (b) Bloquear la salida mientras ScanSaving (PopScope y/o deshabilitar la NavigationBar), que es lo mas barato para el caso concreto. Aparte, saltear la subida a Storage: mientras isStorageUrl no aplique y Storage no este activado, ese await de hasta 45 segundos en el camino caliente de guardar es el que crea la ventana de la carrera.
- **[media]:80** Si falla el guardado, se pierde el borrador entero y todas las correcciones del usuario
  - Falla: La persona escanea, corrige el comercio, el total, la fecha y la categoria en la pantalla de revision, y toca 'Guardar'. El estado pasa a ScanSaving(draft). Si el commit a Firestore falla (sin senal en el celular, PERMISSION_DENIED, sesion vencida) `saveDraft` devuelve Left y el estado pasa a ScanFailed, que NO lleva el draft.

En `_buildBody` ScanFailed no matchea ScanReviewing ni ScanSaving, asi que cae en `_ => _buildPicker(state)`: la pantalla vuelve al selector de foto. El formulario y todo lo tipeado desaparecen, queda solo un snackbar rojo. Para reintentar hay que apretar 'Leer ticket' de nuevo, correr el OCR otra vez y volver a corregir todo a mano. En web con Tesseract eso son otros 20 segundos y el mismo trabajo de nuevo.

Es justo lo contrario de lo que busca el paso de revision: el trabajo de correccion del usuario se tira por un error transitorio de red.
  - Propuesta: Que el fallo del guardado conserve el borrador. Lo mas simple: agregar `class ScanSaveFailed extends ScanState { final ReceiptDraft draft; final String message; }` y en `confirm` devolver `ScanSaveFailed(draft, failure.message)`; en `_buildBody` renderizar ese estado con el mismo `ReceiptReviewForm(draft: ..., isSaving: false)` para que el boton 'Guardar' reintente con las correcciones puestas. Alternativa mas chica: en la rama de fallo de `confirm` volver a `ScanReviewing(draft)` y dejar que el snackbar cuente el error.

### `lib/features/settings/data/account_service.dart`

- **[alta]:36** Borrar la cuenta deja las fotos de todos los tickets en Firestore, para siempre y sin dueño
  - Falla: Usuario con 300 tickets escaneados toca Ajustes > Borrar mi cuenta. `_borrarColeccion('receipts', uid)` borra los 300 documentos de `receipts/{id}`, pero en Firestore borrar un documento NO borra sus subcolecciones: los 300 documentos `receipts/{id}/media/thumb`, con la foto del ticket en base64 (84 a 160 KB cada uno segun ThumbnailService, o sea unos 30 MB acumulados), siguen existiendo. Acto seguido `user.delete()` destruye el uid. A partir de ese momento la regla `allow get/delete: if owns()` (que compara `request.auth.uid == resource.data.userId`) no la puede satisfacer nadie nunca mas, porque ese uid no existe y Firebase no reusa uids. Resultado: la foto de cada compra que hizo la persona queda guardada de forma permanente e indestructible desde la app, despues de que la app le prometio 'Se borran todos tus datos', y ocupando el 1 GiB del plan gratuito sin forma de recuperarlo.
  - Propuesta: En `_borrarColeccion`, cuando la coleccion es `receipts`, agregar al mismo batch el borrado de la miniatura antes de borrar el padre: `batch.delete(doc.reference.collection('media').doc('thumb'))` junto a `batch.delete(doc.reference)` (el id del doc de media es fijo, 'thumb', asi que no hace falta listar la subcoleccion ni pagar lecturas). Como el batch ya tiene tope de 400 documentos y ahora son 2 escrituras por ticket, bajar `_batchSize` a 200. De paso, borrar tambien los archivos de `receipt_images/{uid}/` en Storage por si quedaron de cuando Storage estuvo activo, o dejar escrito que no existen.
- **[alta]:56** Borrar la cuenta no toca `groups` ni `groups/{id}/expenses`: quedan grupos zombi con los gastos de la persona
  - Falla: Usuario crea un grupo, carga 20 gastos compartidos (descripcion, montos, quien pago), y despues borra su cuenta. `_borrarColeccion` filtra siempre por `where('userId', isEqualTo: userId)`, y los documentos de `groups` no tienen campo `userId`: se autorizan por `memberIds`. Ninguna de las cuatro colecciones de la lista es `groups`. Entonces el grupo y sus 20 gastos sobreviven intactos al borrado de cuenta. Peor: `allow delete` en `/groups/{groupId}` exige `request.auth.uid == resource.data.createdBy`, y ese uid acaba de dejar de existir, asi que el grupo es indestructible. Si ademas el grupo llega a tener otro miembro (fase 2, invitaciones), esa otra persona sigue viendo el nombre, la foto y todos los gastos de alguien que pidio que se borrara todo.
  - Propuesta: Antes de `user.delete()`, recorrer `groups where memberIds arrayContains uid`. Para cada grupo: si es el unico miembro, borrar sus `expenses` (paginando de a 400) y despues el documento del grupo; si hay mas miembros, sacarse de `memberIds` y de `members` y dejar el libro (los gastos ya repartidos no se pueden borrar sin descuadrar a los demas). La segunda rama necesita ademas una regla que permita ese `update` de salida, que hoy no existe: `allow update` en /groups exige `request.resource.data.memberIds == resource.data.memberIds`.
- **[media]:36** Borrar la cuenta deja las fotos base64 huerfanas en Firestore, sin nadie que pueda leerlas ni borrarlas
  - Falla: _borrarColeccion recorre solo las colecciones raiz (receipts, expenses, insights, budgets) filtrando por userId. Borrar un documento de Firestore NO borra sus subcolecciones, y receipts/{id}/media/thumb no aparece en ninguna parte del borrado. Escenario: un usuario con 500 tickets (unos 50 MB de base64 segun la medicion de 84-160 KB por ticket) toca 'borrar cuenta'. Se borran los tickets y los gastos, se borra el usuario de Auth, y quedan 500 documentos media/thumb con las fotos de sus compras. A partir de ahi son irrecuperables e imborrables desde la app: todas las reglas de media (firestore.rules:71-76) piden owns(), es decir un uid que ya no existe y con el que nadie va a poder autenticarse nunca. El propio comentario del metodo describe este riesgo ('quedarian huerfanos para siempre y sin nadie que pueda leerlos ni borrarlos') y no lo cubre para media. Consecuencias: la promesa de 'borrar cuenta con todos sus datos' es falsa, y el espacio consumido no se recupera nunca del 1 GiB del plan gratuito. Lo mismo pasa con los grupos: deleteAccount no toca groups ni groups/{id}/expenses, asi que quedan el displayName, la photoUrl y el historial de gastos de la persona que se fue.
  - Propuesta: En _borrarColeccion, cuando la coleccion es receipts, agregar al batch el delete de doc.reference.collection('media').doc('thumb') junto con el ticket (mismo patron que deleteReceipt). Si en algun momento media pasa a tener mas de un documento por ticket, hay que listarlos, pero hoy alcanza con 'thumb'. Aparte, sacar al usuario de memberIds de sus grupos antes de borrar la cuenta, o el grupo queda con un miembro fantasma.
- **[media]:116** El borrado de cuenta borra los datos primero y solo sabe reautenticar con Google: un usuario de email queda sin datos y con la cuenta viva
  - Falla: Persona que se registro con `registerWithEmail` (auth_provider.dart:78) y tiene la sesion abierta hace mas de unos minutos toca Borrar mi cuenta. `deleteAccount()` borra receipts, expenses, insights, budgets y el doc de users (irreversible, sin backup, el CSV no incluye tickets). Recien despues llama a `user.delete()`, que tira `requires-recent-login`. La pantalla entonces llama a `reauthenticate()`, que en web hace SIEMPRE `reauthenticateWithPopup(GoogleAuthProvider())`: para una cuenta de email/contraseña eso falla (user-mismatch o provider no vinculado), asi que `ok` es false y NO se reintenta el borrado. La persona termina con todos sus gastos y tickets borrados, la cuenta todavia existente, y un cartel rojo que dice 'No se pudo reautenticar'. Repetir la operacion tampoco arregla nada: los datos ya no estan y la cuenta sigue sin poder borrarse desde la app.
  - Propuesta: Dos cosas. (1) Verificar la sesion fresca ANTES de borrar nada: hacer `user.delete()` como primer paso solo si ya se comprobo la reautenticacion, o mejor, forzar el reauth siempre al inicio del flujo y recien despues empezar a borrar datos. (2) Elegir el proveedor segun `user.providerData.first.providerId`: si es 'password' pedir la contraseña y usar `EmailAuthProvider.credential(...)` con `reauthenticateWithCredential`; si es 'google.com' el popup. Si el proveedor no se reconoce, no borrar nada y decirlo.
- **[media]:116** Reautenticar para borrar la cuenta siempre usa popup de Google: quien se registro con email no puede borrarse nunca
  - Falla: Firebase pide sesion fresca para user.delete(). La app detecta 'requires-recent-login' y reautentica, pero en web siempre con reauthenticateWithPopup(GoogleAuthProvider()), sin mirar con que proveedor se logueo la persona. Caso 1: una cuenta creada con createUserWithEmailAndPassword (la pantalla de registro existe) toca borrar, Firebase pide sesion fresca, se abre el popup de Google y falla con user-mismatch o credential-already-in-use, y el snackbar dice 'No se pudo reautenticar'. La cuenta no se puede borrar desde la UI, nunca. Caso 2, en Safari de iPhone y en el PWA instalado: el popup se abre despues del dialogo de confirmacion y despues del await de deleteAccount, o sea a varios gaps asincronicos del gesto del usuario, que es exactamente lo que el bloqueador de popups de Safari mata; el error es popup-blocked y tampoco hay forma de borrar la cuenta.
  - Propuesta: Ramificar por proveedor real: recorrer user.providerData y si aparece 'password' pedir la contraseña en un dialogo y usar reauthenticateWithCredential(EmailAuthProvider.credential(...)); si aparece 'google.com' usar el popup. Para el popup en web, capturar popup-blocked y popup-closed-by-user y caer a reauthenticateWithRedirect, y aclararlo en el mensaje al usuario en vez de mostrar el error crudo.

### `lib/services/ocr/receipt_text_parser.dart`

- **[alta]:42** El total puede salir de la linea SIGUIENTE al keyword: en carniceria.txt recortado da $22,00 en vez de $1.056,00
  - Falla: Verificado ejecutando el parser. Tomo test/assets/ocr/carniceria.txt y le saco la ultima linea del total (foto cortada abajo, sombra, o el OCR no leyo esa fila: es el caso mas comun en un e-Ticket largo). El texto queda terminando en la tabla de IVA:

    TIPO  NETO  IVA  Sub TOTAL
    22%  176.72  38.88  215.60
    ...

Resultado real: totalCents = 2200, o sea $22,00 en vez de $1.056,00. El '22' sale de '22%' de la fila siguiente, porque el `\s*` entre 'TOTAL' y el monto matchea el salto de linea y se come el encabezado de columna 'Sub TOTAL'.

Mismo experimento con panaderia.txt sin la ultima linea: totalCents = 1000 ($10,00 en vez de $411,14), esta vez el 'Importe' del encabezado 'Precio  Importe' agarra el '10' de '10 101  CATALANES REDONDOS'.

Es el peor modo de falla posible: el formulario de revision se abre con $22,00 ya escrito como si el OCR lo hubiera leido bien, y ese es el monto que va al gasto y al presupuesto. En el ticket completo el bug esta igual (el match 'Sub TOTAL\n22' ocurre), solo que queda tapado porque `extractTotal` se queda con el ultimo match.
  - Propuesta: Que el monto tenga que estar en la MISMA linea que el keyword: cambiar los tres `\s*` por `[^\S\n]*` (espacio horizontal, sin salto de linea). Eso solo rompe el caso legitimo 'TOTAL A PAGAR:' con el monto en la linea de abajo, asi que hay que reponerlo aparte: si la linea del keyword no trae monto, mirar la linea siguiente y aceptarla SOLO si es un monto solo (`^\s*[\$€£]?\s*(monto)\s*$`). Con esa condicion '22%  176.72  38.88  215.60' y '10 101  CATALANES REDONDOS  14.77' quedan descartadas, y '250,00' sola sigue funcionando. Agregar ademas un test con cada fixture truncado a la ultima linea, que es como llega la mitad de las fotos.
- **[alta]:190** Una fila de IVA impresa DESPUES del total gana, porque extractTotal se queda con el ultimo match
  - Falla: Verificado ejecutando el parser sobre:

    SUPERMERCADO X
    10/03/2026
    Leche 2 L        150,00
    TOTAL          1.234,00
    IVA TASA BASICA TOTAL   222,00

Resultado real: totalCents = 22200, o sea $222,00 en vez de $1.234,00. La regla 'el total pagable esta abajo de todo' no aguanta los tickets que imprimen el desglose de impuestos DESPUES del total, que es un formato comun en Uruguay ('IVA incluido', 'TOTAL IVA', 'IMPORTE IVA'). Los tres fixtures que hay ponen la tabla de IVA ARRIBA del total, asi que ningun test cubre este orden.

Se guarda un gasto de $222 por una compra de $1.234 y el presupuesto del mes queda mal en silencio.
  - Propuesta: Descartar los candidatos cuya linea sea contabilidad de impuestos antes de aplicar 'el ultimo gana': si la linea del match matchea `^\s*(?:iva|i\.v\.a|impuesto|tipo|neto|sub\s*-?\s*total)` no es el total pagable. Y como red de seguridad, cuando hay varios candidatos y el ultimo es menor que la suma de los items detectados, quedarse con el mayor en vez de con el ultimo: ningun total pagable puede ser menor que lo que ya se detecto en el detalle.
- **[media]:259** La rama de items con cantidad (`3 x 500`) no aplica el filtro `precio > total`, y una medida tipo `40x60` se lee como cantidad
  - Falla: Verificado ejecutando el parser sobre:

    FERRETERIA CENTRAL
    01/02/2026
    Bolsa 40x60         35,00
    Lija 3 x 25,00      75,00
    TOTAL              110,00

Resultado real: item 'Bolsa' con quantity=40.0, unitPriceCents=6000, totalPriceCents=240000. O sea un item de $2.400,00 dentro de un ticket de $110,00, y ademas se perdio el precio real (35,00). La medida '40x60' matchea `_itemQtyPattern` como 'cantidad 40 x precio 60'.

La causa es que el guard documentado ('ningun item puede costar mas que el ticket entero', docs/estado-y-decisiones.md) esta escrito SOLO en la rama de abajo (linea 295) y la rama de qty hace `continue` antes de llegar. Cualquier producto con medidas ('Toalla 50x70', 'Cable 2x1.5', 'Bolsa 40x60') entra sin control. Los items se persisten en `receipts/{id}.items` y son la base de la asignacion por item de grupos fase 3.
  - Propuesta: Aplicar el mismo guard en la rama de qty: descartar el item si `totalCents != null && (qty * unitCents).round() > totalCents`. Y exigir separacion en el patron para que una medida no pase por cantidad: pedir al menos un espacio de cada lado de la x (`\s+[x×*]\s+`, dejando `×` y `*` sin ese requisito si se quiere), porque '40x60' pegado nunca es 'cuarenta unidades a sesenta'.
- **[baja]:238** Sin marcador de inicio, el bloque de items se come el encabezado y la direccion entra como producto
  - Falla: Verificado ejecutando el parser sobre un ticket no fiscal (los que no dicen 'MONEDA', 'Cant.', 'Descripcion' ni 'Pesos Uruguayos', o donde el OCR no leyo esa fila):

    SUPERMERCADO EL FARO
    Av. Italia 12
    25/12/2025
    Coca Cola 2L      120,00
    Pan               80,00
    TOTAL             200,00

Resultado real: 3 items, y el primero es 'Av. Italia' por $12,00. Como no aparece ningun `_itemSectionStartPattern`, `start` queda en 0 y el bloque arranca en la primera linea del ticket. El filtro `cents > totalCents` limpia el RUT y el CAE porque son numeros gigantes, pero no limpia una altura de calle chica, un numero de caja, ni un telefono corto: cualquier numero del encabezado menor al total pasa como producto.

No corrompe el total (que es lo que alimenta el gasto), pero ensucia `receipts/{id}.items`, que es la base de la asignacion por item de grupos fase 3.
  - Propuesta: Cuando no hay marcador de inicio, arrancar despues del encabezado en vez de en la linea 0: saltar hasta pasada la ultima linea que ya se uso como comercio o como fecha (extractStoreName mira las primeras 4 lineas, extractDate ya ubica la linea de la fecha). Complementario y barato: descartar como item cualquier linea que matchee `_taxIdLabelPattern` o que empiece con un prefijo de direccion tipico (`av\.?|avda|bvar|calle|ruta|dr\.?|ing\.?`).

### `lib/services/ocr/tesseract/tesseract_ocr_service_web.dart`

- **[alta]:46** El timeout de 20 s de Tesseract hace fallar el escaneo real en el navegador del celular
  - Falla: Primer escaneo en el celular, con la foto de 1080x1920 que devuelve el picker. Los 20 s tienen que cubrir TODO: bajar el core wasm de tesseract.js y los traineddata de 'eng+spa' desde tessdata.projectnaptha.com (entre 8 y 15 MB la primera vez, sin cachear en el service worker porque son de otro origen), y ademas correr el OCR de una imagen de ~2 MP, que en un celular de gama media son 10 a 40 s por si solo. Se dispara TimeoutException, se envuelve en OcrException y la persona ve el snackbar 'No se pudo leer el ticket: Failed to process image: TimeoutException after 0:00:20.000000'. La segunda vez a veces anda porque tesseract.js cachea el traineddata en IndexedDB, o sea que el sintoma es 'el primer intento siempre falla', el peor tipo de bug para confiar en la app. La funcion central del producto es la que se rompe, y justo en el unico lugar donde el usuario la usa.
  - Propuesta: Separar la carga del motor del reconocimiento: usar createWorker() una vez (precalentado al entrar a /scan, con su propio indicador) y reusar el worker, en vez de Tesseract.recognize() que arma y tira todo en cada llamada. Servir corePath y langPath desde assets propios en web/ para que el service worker los cachee y el PWA funcione offline y sin depender de un CDN de terceros. Para el reconocimiento, o sacar el timeout y mostrar progreso real (Tesseract emite logger con progreso), o subirlo a 120 s. Ademas conviene achicar la imagen a ~1000-1200 px de ancho antes de mandarla al OCR: Tesseract escala con los pixeles y el detalle del ticket no mejora arriba de eso.

### `firestore.rules`

- **[media]:72** Cualquier usuario autenticado puede crear documentos dentro de receipts/{ticketAjeno}/media, y la victima no puede leerlos ni borrarlos
  - Falla: La regla de create de la subcoleccion media solo pide claimsOwnership(), o sea que el documento QUE ESCRIBO lleve MI userId. No verifica nada sobre el ticket padre. El path receipts/{receiptId}/media/{mediaId} tiene mediaId libre. Escenario: soy miembro de un grupo con la victima; los gastos de grupo guardan `receiptId` en claro (group_expense_model.dart:60, campo receiptId) y todos los miembros los leen, asi que conozco ids de tickets ajenos. Con el SDK escribo receipts/{receiptIdDeLaVictima}/media/x con {userId: <mi uid>, data: <600 KB de base64>}, y lo repito con mediaId distinto cuantas veces quiera. Las reglas lo aceptan. La victima no puede leerlos (get pide owns(), y el userId del documento es el mio), no puede borrarlos (delete pide owns()), y su propio borrado de ticket solo toca el id 'thumb' (receipt_repository_impl.dart:81). Quedan para siempre dentro del subarbol de SU ticket, consumiendo el 1 GiB del plan Spark que es la premisa de todo el diseno de fotos: ~1.700 documentos y la base se llena para todos. Ademas, si el ticket de la victima quedo sin thumb, puedo ocupar yo el id 'thumb' y su propia app deja de poder escribirlo (set sobre documento existente es update, y update pide owns()).
  - Propuesta: Atar el media al dueno del ticket padre y acotar el id. Escribir una miniatura es raro comparado con leerla, asi que la lectura extra se paga sin problema, el mismo argumento que ya usan las reglas de groups/{id}/expenses en las lineas 141-145: `allow create: if claimsOwnership() && mediaId == 'thumb' && get(/databases/$(database)/documents/receipts/$(receiptId)).data.userId == request.auth.uid && request.resource.data.data is string && request.resource.data.data.size() < 700000;`. La lectura por owns() se puede dejar como esta, que no cuesta nada.
- **[media]:146** Las reglas de gastos de grupo no validan shares ni paidBy: un miembro puede cargarle a otro plata que no gasto
  - Falla: El create de groups/{groupId}/expenses valida memberIds, createdBy, amountCents y date, pero de shares y paidBy no dice absolutamente nada: ni que existan, ni que sean mapas, ni que sus claves sean miembros del grupo. El unico control de que el reparto cierre esta en el cliente (group_repository.dart:120-162: validacion de paidBy, de participantes y el chequeo gasto.isBalanced), y firestore.rules es la unica capa de autorizacion. Escenario: grupo con Ana y yo. Desde la consola o con un cliente modificado escribo un gasto con {amountCents: 100000, paidBy: {yo: 100000}, shares: {ana: 100000}}. Las reglas lo aceptan. El libro es la fuente de los balances (pairwiseDebts deriva todo de los gastos), asi que a Ana le aparece que me debe 1.000 pesos por algo que no consumio, sin ningun rastro de que el documento este mal formado. Peor variante silenciosa: shares que no suman amountCents. El grupo nunca cierra en cero y nadie entiende por que, que es exactamente el problema que el metodo del resto mayor existe para evitar. Hoy no es explotable porque los grupos tienen una sola persona, pero es la parte del feature que el gatillo bloqueante manda cerrar ANTES de que entre gente ajena, y no aparece en 'Lo que NO esta verificado'.
  - Propuesta: Las reglas no pueden sumar un mapa, pero si pueden acotar el dano: exigir `request.resource.data.shares is map && request.resource.data.paidBy is map && request.resource.data.shares.keys().hasOnly(grupoDe(groupId).memberIds) && request.resource.data.paidBy.keys().hasOnly(grupoDe(groupId).memberIds)`, y lo mismo en el update contra resource.data.memberIds. Con eso al menos nadie puede meter uids ajenos al grupo en el reparto. La suma exacta queda como invariante del cliente, y conviene dejarlo escrito como limitacion conocida en docs/grupos-y-division.md.
- **[media]:153** El update de gastos de grupo no valida tipos: un miembro puede dejar la pantalla del grupo rota para todos
  - Falla: El update de groups/{groupId}/expenses solo exige ser del grupo, que memberIds no cambie y que amountCents sea centavos validos. No valida `date is timestamp` (el create si lo hace) ni el tipo de paidBy y shares. Escenario: un miembro del grupo (o alguien con un cliente propio) hace update de un gasto poniendo date: 'hoy' o paidBy: 5. Las reglas lo aceptan. En cada cliente, GroupExpenseModel.fromFirestore hace `(data['date'] as Timestamp?)` (linea 29) y `(raw as Map<String, dynamic>?)` (linea 46): el cast tira TypeError, la excepcion sale del map del stream y groupExpensesProvider queda en error para TODOS los miembros. La pantalla del grupo muestra 'No se pudieron cargar los gastos' y no hay forma de arreglarlo desde la UI, porque la lista no renderiza y el boton de borrar ese gasto vive dentro de la lista. El grupo queda inutilizable de forma permanente para todos sus miembros.
  - Propuesta: Repetir en el update las validaciones de tipo del create: `&& request.resource.data.date is timestamp && request.resource.data.createdBy == resource.data.createdBy` (createdBy tampoco es inmutable hoy), mas los chequeos de shares/paidBy del hallazgo anterior. En el cliente, ademas, envolver fromFirestore para que un documento corrupto se saltee en vez de tumbar el stream entero.
- **[baja]:133** Borrar un grupo deja sus gastos legibles para siempre y imposibles de borrar
  - Falla: El delete de groups/{groupId} lo permite el creador, y borrar el documento del grupo no borra su subcoleccion expenses. Una vez borrado el grupo: los gastos siguen siendo legibles por list/get (firestore.rules:139-140 usa la copia desnormalizada resource.data.memberIds, que no depende del grupo), pero toda escritura pasa por esDelGrupo(groupId), que hace get() del grupo inexistente y accede a .memberIds sobre data null, lo que corta la evaluacion y deniega. Escenario: el creador de un grupo con tres personas borra el grupo desde el SDK. Las descripciones, montos y repartos de todos siguen ahi, accesibles para cualquiera que este en la copia de memberIds, y nadie, ni el creador, puede borrarlos jamas. Nada en lib/ llama a un deleteGroup: es una regla que el codigo no usa y que solo abre el agujero.
  - Propuesta: Mientras no exista un borrado que limpie la subcoleccion (sin Cloud Functions eso significa borrar los gastos desde el cliente antes del grupo), sacar el `allow delete` de groups/{groupId} y usar el campo `archived` que el modelo ya tiene. Si se quiere mantener el delete, exigir en la regla que el grupo este archivado y que la app borre primero los gastos.

### `lib/core/format/money.dart`

- **[media]:66** `Money.parse` se come el signo menos: un monto negativo se guarda positivo
  - Falla: El `replaceAll(RegExp(r'[^\d.,]'), '')` borra todo lo que no sea digito, punto o coma, incluido el `-`. Verificado: `Money.parse('-100')` devuelve 10000 y `Money.parse('-1.234,56')` devuelve 123456. Escenario: en la web de escritorio el campo de monto es un input de texto comun (`numberWithOptions` no bloquea el guion). La persona quiere anotar una devolucion y escribe `-1500` en 'Nuevo gasto'. `add_expense_sheet._save` recibe 150000, pasa la validacion `<= 0` y `addManualExpense` guarda un gasto de +$1.500. El mes queda inflado en $3.000 respecto de lo real (la devolucion no resto y ademas sumo), el presupuesto de la categoria se pasa antes de tiempo, y no hubo ningun aviso.
  - Propuesta: Detectar el signo antes de limpiar y aplicarlo al resultado, o -- si los montos negativos no son un caso soportado -- devolver `null` cuando la entrada contiene `-`, para que la UI muestre 'Pone un monto valido' en vez de invertir el signo en silencio. Lo que no puede quedar es que el usuario escriba una cosa y se guarde la contraria.
- **[baja]:86** `Money.parse` devuelve 0 (no null) para entradas que son solo separadores
  - Falla: El doc dice 'Devuelve null si no se puede interpretar', pero cuando la parte entera queda vacia se sustituye por '0' y `double.tryParse('0.')` en Dart devuelve 0.0. Verificado: `Money.parse('.')`, `Money.parse(',')`, `Money.parse('...')` y `Money.parse(',,,')` devuelven todos 0, no null. Los llamadores que validan `<= 0` (add_expense_sheet, receipt_review_form, budget_sheet) lo atajan; el que valida solo `== null` no: en `receipt_detail_screen.dart:486` una coma suelta en el campo Total se guarda como un ticket y un gasto de $0 (ver hallazgo del editor de tickets). Es el mismo bug visto desde el otro lado: la funcion incumple su contrato y deja que un solo llamador descuidado pierda plata.
  - Propuesta: Devolver `null` cuando `s` no contiene ningun digito (`if (!s.contains(RegExp(r'\d'))) return null;` justo despues del `isEmpty`). Agregar los casos `'.'`, `','`, `'...'` al grupo 'entrada invalida devuelve null' de money_test.dart, que hoy solo prueba `''` y `'abc'`.

### `lib/core/platform/file_saver_web.dart`

- **[media]:28** La descarga del CSV revoca el blob antes de que el navegador lo lea, y la app igual dice que exporto
  - Falla: Se hace click() sobre el ancla y en la misma vuelta sincronica se lo saca del DOM y se llama revokeObjectURL. Chrome de escritorio suele salvarse porque arranca la descarga durante el click, pero en Safari (escritorio y iOS, que es donde el usuario esta) y en Firefox la descarga se inicia despues y la URL ya no existe: no baja nada, o baja un archivo vacio. Y lo peor es lo que ve la persona: saveFile devuelve null igual, y el codigo de settings interpreta null como 'salio bien' y muestra '37 gastos exportados' en verde. Falla silenciosa sobre un respaldo: el usuario cree que tiene su CSV guardado, y ademas el dialogo de borrar cuenta le dice 'si querés quedarte con tus datos, exportalos a CSV antes'.
  - Propuesta: Dejar el ancla en el DOM y postergar la limpieza: despues del click(), programar la baja del ancla y el revokeObjectURL con un Future.delayed de ~1 s (o en el evento de siguiente frame). El null que devuelve saveFile en web no significa 'ok', significa 'no hay ruta que mostrar': conviene que la implementacion web devuelva un bool de exito, o al menos que el mensaje diga 'Se envio el archivo a tus descargas' en vez de afirmar que quedaron exportados.

### `lib/features/auth/presentation/providers/auth_provider.dart`

- **[media]:99** Cerrar sesion lanza UnmountedRefException en un future sin await
  - Falla: El usuario entra a Ajustes y toca 'Cerrar sesion'. SettingsScreen hace ref.read(authProvider.notifier).signOut() sin await y sin watchear authProvider en ningun lado (solo watchea currentUserProvider, que es otro provider). authProvider es autoDispose, asi que queda disposed a los ~2 turnos del event loop, mientras FirebaseAuth.signOut() todavia esta limpiando la sesion. Al volver del await, `state = const AsyncValue.data(null)` corre sobre un Ref disposed y tira UnmountedRefException, que al no haber await queda como error asincronico no capturado. La sesion se cierra igual, pero cada logout deja una excepcion sin manejar en la consola, y si manana alguien encadena algo despues de ese setter, no se va a ejecutar nunca.
  - Propuesta: Agregar `if (!ref.mounted) return;` antes del setter, igual que en los otros metodos del mismo notifier. Y en settings_screen.dart, hacer el onTap async con await y try/catch para no dejar el future colgando.

### `lib/features/groups/domain/balance.dart`

- **[media]:97** Con mas de un pagador, las deudas de a pares no suman el balance neto: falta o sobra un centavo
  - Falla: Cada deudor reparte su deuda entre los acreedores con `splitLargestRemainder` de forma INDEPENDIENTE. Eso garantiza que la fila de cada deudor cierre, pero no que la columna de cada acreedor cierre: la suma de lo que un acreedor recibe puede no ser lo que le corresponde. Contraejemplo minimo (verificado ejecutandolo): un gasto de 4 centavos, `paidBy = {A:1, B:3}`, `shares = {C:2, D:2}`. C reparte sus 2 entre [1,3]: exacto 0,5 y 1,5, base [0,1], restos empatados en 2, desempate por indice -> [1,1]. D igual -> [1,1]. Resultado: A recibe 2 pero su neto es +1; B recibe 2 pero su neto es +3. En la pantalla del grupo B lee arriba 'Te deben' con el numero de `netBalances` y justo abajo la lista de a pares (`debtsInvolving`) que suma un centavo menos. Un fuzz de 200.000 grupos aleatorios lo reproduce con montos reales (neto 918.262 vs pairwise 918.263). Con un solo pagador el mismo fuzz no encuentra ni un descuadre, asi que hoy la hoja de calculo cierra solo porque `add_group_expense_sheet.dart:223` manda `paidBy: {_pagador: _montoCents}`. Es alcanzable ya por `GroupRepository.addExpense` (acepta cualquier `paidBy`) y `GroupExpenseModel.fromFirestore` lee documentos multi-pagador; el modelo esta documentado como 'soporta que paguen varios' y la fase 2 se apoya en esto.
  - Propuesta: Repartir la matriz deudor x acreedor de una sola vez en vez de fila por fila, para que cierren filas Y columnas. Lo mas simple y ya disenado en docs/grupos-y-division.md: correr el greedy de `settle` DENTRO de cada gasto sobre (acreedores, deudores) -- las dos listas suman lo mismo, asi que las transferencias que produce son exactas por construccion y ademas dan a lo sumo n-1 aristas. Agregar como test la invariante: para todo uid, `sum(pairwiseDebts recibidas) - sum(pagadas) == netBalances[uid]`.

### `lib/features/groups/presentation/screens/group_detail_screen.dart`

- **[media]:238** Borrar un gasto de grupo descarta el Either: si falla no pasa nada visible
  - Falla: El usuario deja apretado un gasto del grupo, confirma en el dialogo que dice 'se borra para todos', y deleteExpense devuelve Left (sin red, o las reglas rechazan que borre un gasto que no creo el). El resultado se descarta: no hay snackbar, no hay error, el dialogo ya se cerro y la fila sigue en la lista porque el snapshot nunca cambio. El usuario lo interpreta como que el long press no registro y lo repite. Con grupos esto va a pasar de verdad, porque el gasto lo puede haber cargado otra persona.
  - Propuesta: Plegar el resultado y mostrar el fallo:
  final res = await ref.read(groupRepositoryProvider).deleteExpense(gasto.groupId, gasto.id);
  if (!context.mounted) return;
  res.fold(
    (f) => ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(f.message))),
    (_) {},
  );

### `lib/features/insights/domain/entities/monthly_spending_summary.dart`

- **[media]:107** MerchantTotal.signature tiene los $ escapados: devuelve siempre la misma constante y rompe la igualdad del resumen mensual
  - Falla: En Dart, '\$name' es el texto literal '$name', no una interpolacion. Asi que signature devuelve el string constante '$name:$totalCents:$visits' para TODO MerchantTotal, y props solo distingue por la cantidad de comercios. Escenario: en el mes hay dos gastos de $500 en 'Comida', uno en Disco y otro cargado como 'Tienda Inglesa'. El usuario corrige ese ticket y le pone 'Devoto'. Llega el snapshot, currentMonthSummary recalcula, pero month, totalCents, expenseCount, _categorySignature y el join de signatures son identicos: el nuevo resumen es == al viejo, Riverpod no notifica a los dependientes, y monthlyInsights sigue sirviendo el texto cacheado que nombra 'Tienda Inglesa'. Lo mismo si dos comercios de la misma categoria se intercambian montos. Justo el bug de '\$' que docs/estado-y-decisiones.md marca como trampa conocida, y el grep que propone (`grep -rn '\\${' lib`) no lo detecta porque aca no hay llaves.
  - Propuesta: Sacar los backslashes: '$name:$totalCents:$visits'. Mejor todavia: MerchantTotal ya extiende Equatable con props [name, totalCents, visits], asi que el getter signature sobra. Poner `topMerchants` directo en props de MonthlySpendingSummary y borrar signature. Agregar un test que compare dos resumenes que solo difieren en el nombre del comercio y verifique que NO son iguales.
- **[media]:107** `MerchantTotal.signature` es un literal, no una interpolacion: todos los comercios tienen la misma firma
  - Falla: En Dart `'\$name'` dentro de comillas simples es un escape valido: el string es literalmente `$name:$totalCents:$visits`, igual para todo comercio (verificado ejecutandolo). `MonthlySpendingSummary.props` usa `topMerchants.map((m) => m.signature).join(',')`, asi que ese elemento de la igualdad solo codifica CUANTOS comercios hay, no cuales ni por cuanto. Escenario: en marzo hay un solo comercio, 'TIENDA INGLESA S A' por $14.000. La persona corrige el nombre desde el editor del ticket a 'Tienda Inglesa'. `totalCents`, `expenseCount` y las categorias no cambian, y la cantidad de comercios sigue en 1 -> los dos resumenes son `==` -> `currentMonthSummaryProvider` no notifica -> `monthlyInsights` no se recomputa y sigue citando el nombre y los montos viejos. Es exactamente la trampa del `\$` que documenta estado-y-decisiones.md, y el grep que ahi se propone (`grep -rn '\\${'`) no la detecta porque no hay llaves. Los tests de `monthly_spending_summary_test.dart` tampoco: el caso 'un gasto nuevo si cambia la igualdad' pasa por el cambio de `totalCents`, no por la firma.
  - Propuesta: `String get signature => '$name:$totalCents:$visits';` (comillas simples estan bien, hay que sacar las barras). Agregar un test que cambie SOLO el nombre del comercio y espere `isNot(equals(...))`. Y ampliar el grep de la doc a `grep -rn "\\\\\$[a-zA-Z_{]" lib --include="*.dart"`, que si lo encuentra.

### `lib/features/receipts/domain/entities/receipt_draft.dart`

- **[media]:64** Si el usuario borra el comercio en la revision, se guarda igual el que leyo el OCR
  - Falla: Verificado con un test: `draft.copyWith(storeName: null)` devuelve el storeName viejo, no null.

Escenario: el OCR agarra una linea equivocada como comercio (con un texto que no es un ticket el parser devolvio 'Estimado cliente'; con un ticket de un comercio chico agarra la direccion o el numero de resolucion). La persona ve el campo 'Comercio' con esa basura, lo borra a proposito para dejarlo vacio y toca Guardar.

`_confirm` hace exactamente lo correcto: `storeName: store.isEmpty ? null : store`. Pero `copyWith` usa `storeName ?? this.storeName`, asi que ese null se descarta y se persiste el nombre malo, tanto en `receipts/{id}.storeName` como en el `expenses` del batch. Aparece en la lista, en el detalle, en 'comercios con mas gasto' del insight y en el CSV exportado. La correccion del usuario se ignora en silencio: el patron `??` no distingue 'no toques esto' de 'ponelo en null'.
  - Propuesta: Distinguir 'ausente' de 'null' para los campos nullable. Lo mas directo en Dart: un sentinel, `static const _sinCambio = Object();` con `Object? storeName = _sinCambio` y `storeName: identical(storeName, _sinCambio) ? this.storeName : storeName as String?`. Alternativa mas liviana: un flag explicito `bool clearStoreName = false`. Aplica igual a `receiptDate` y `totalCents`, que hoy tampoco se pueden vaciar.
- **[baja]:64** Borrar el nombre del comercio en la pantalla de revision no borra nada: se guarda el que leyo el OCR
  - Falla: En web el OCR es Tesseract y equivoca seguido el comercio (por ejemplo deja 'RUT 216894130017' o una linea de encabezado como nombre). La pantalla de revision existe justo para eso. Si la persona vacia el campo Comercio y toca Guardar, _confirm manda storeName: null, y copyWith resuelve `storeName ?? this.storeName`, o sea que se queda con el valor del OCR. El ticket y el gasto se guardan con el nombre equivocado que la persona acaba de borrar, sin ningun aviso, y despues aparece asi en la lista, en el CSV y en el ranking de comercios de los insights.
  - Propuesta: Distinguir 'no lo toques' de 'ponelo en null' en copyWith. Lo mas simple es un flag: `ReceiptDraft copyWith({..., bool clearStoreName = false})` y usar `storeName: clearStoreName ? null : (storeName ?? this.storeName)`; o pasar un sentinela (Object? storeName = _unset). El call site en receipt_review_form.dart:_confirm ya sabe cuando el campo quedo vacio.

### `lib/features/receipts/presentation/screens/receipt_detail_screen.dart`

- **[media]:69** Editar el total de un ticket descarta el Either: si la escritura falla, la hoja se cierra como si hubiera guardado
  - Falla: El OCR leyo mal y guardo un total de $12.345,00 cuando el ticket decia $1.234,50. El usuario abre el detalle, toca editar, corrige el total y guarda. updateReceipt() falla (por ejemplo la query de gastos vinculados es rechazada por reglas, o no hay red). El Either que devuelve se descarta, la hoja hace Navigator.pop, el spinner desaparece y ref.invalidate vuelve a leer el documento sin cambios: la pantalla muestra otra vez $12.345,00 y ningun mensaje. El usuario cree que la correccion no se aplico por un error de UI y el gasto sigue inflado en el total del mes y en el presupuesto de la categoria.
  - Propuesta: Hacer que onSave devuelva Future<String?> con el mensaje de error, plegar el Either y solo cerrar la hoja e invalidar cuando venga Right. En Left, mostrar un snackbar con failure.message y dejar la hoja abierta con los datos escritos. De paso, _save() valida `total == null` pero no `total <= 0`: agregar la comprobacion para que no se pueda dejar un ticket en cero.
- **[media]:486** Editar un ticket acepta total 0 y le borra la plata al gasto asociado, sin avisar
  - Falla: `_EditReceiptSheet._save` solo rechaza `total == null`. Los otros dos puntos de entrada de montos (`receipt_review_form.dart:203` y `budget_sheet.dart:155`) validan `total == null || total <= 0`; este no. Escenario: la persona abre un ticket de $2.480, toca editar, borra el total y escribe `0` (o deja una coma suelta: `Money.parse(",")` devuelve 0, no null, verificado). Se guarda sin error. `updateReceipt` escribe `totalCents: 0` en el ticket Y `amountCents: 0` en el gasto vinculado, en el mismo batch. El mes pierde $2.480, el presupuesto de la categoria se descuenta, el ticket sigue apareciendo en la lista como si estuviera bien, y no hay forma de recuperar el monto original.
  - Propuesta: `if (total == null || total <= 0)`, con el mismo mensaje en espanol que el resto de la app ('Pone un monto valido'). El repositorio tambien deberia rechazarlo: `updateReceipt` no valida `totalCents` aunque `addManualExpense` y `updateExpense` si validan `<= 0`.
- **[media]:254** El detalle del ticket redecodifica la foto y crea un MemoryImage nuevo en cada build
  - Falla: base64Decode y MemoryImage(...) estan dentro del builder del Consumer, o sea que corren en cada rebuild. MemoryImage no define == por contenido (compara la referencia del Uint8List), asi que cada build produce una clave distinta para el ImageCache: no hay hit, se redecodifica el JPEG de ~1000x1300 y se agrega una entrada nueva de ~5 MB descomprimidos, quedandose las anteriores vivas hasta que el cache llega a su tope de 100 MB / 1000 entradas. El screen hace setState en cada editar y en cada borrar, y el provider del detalle se invalida al guardar. Escenario: abrir un ticket y corregir el total tres o cuatro veces deja decenas de MB de copias de la misma foto en el ImageCache; en Safari de iPhone, donde la pestaña tiene un techo de memoria duro y ademas esta el heap de CanvasKit, esto se manifiesta como recarga de la pagina ('A problem repeatedly occurred') o como saltos de jank de cientos de ms en cada rebuild.
  - Propuesta: Decodificar una sola vez y memoizar el ImageProvider: que receiptThumbnailProvider devuelva directamente el Uint8List (o el MemoryImage) ya decodificado, o guardar el MemoryImage en el State del screen la primera vez que llega el base64. Ademas conviene pasarle cacheWidth/resizeWidth al Image de la vista previa de 240 px de alto, como ya se hace en el scan, para no tener el bitmap completo en memoria para una banda recortada.

### `lib/features/receipts/presentation/screens/scan_receipt_screen.dart`

- **[baja]:92** Navegar fuera de /scan durante la revision descarta el borrador sin avisar
  - Falla: `scanProvider` es autoDispose (confirmado en receipt_provider.g.dart: `isAutoDispose: true`), y /scan vive dentro del ShellRoute con la barra inferior siempre visible. Con el ticket ya leido y corregido en pantalla, si la persona toca 'Tickets' en la barra (por ejemplo para chequear si ya lo habia cargado) el ScanReceiptScreen se desmonta, el provider se queda sin oyentes y se destruye: el estado vuelve a ScanIdle. Encima `_selectedImage` y `_selectedImageBytes` viven en el State del widget, asi que tambien se van.

Al volver a 'Escanear' aparece el selector vacio, sin foto, sin borrador y sin ningun mensaje. Hay que elegir la foto de nuevo, correr el OCR de nuevo (en web, Tesseract otra vez) y corregir todo de nuevo.

No pierde nada guardado, pero tira trabajo del usuario en el unico paso donde el usuario trabaja, y sin ninguna senal de que algo se descarto.
  - Propuesta: Marcar el notifier como `@Riverpod(keepAlive: true)` para que el borrador sobreviva a salir de la pantalla (el `reset()` de guardado y de descarte ya lo limpian explicitamente), y guardar `_selectedImageBytes` en el propio ScanState en vez del State del widget. Si se prefiere no mantenerlo vivo, al menos poner un `PopScope`/confirmacion cuando el estado es ScanReviewing, para que descartar sea una decision y no un efecto secundario de tocar la barra de navegacion.

### `lib/features/settings/domain/csv_exporter.dart`

- **[baja]:67** El CSV exporta mal los montos negativos: pierde el signo o inventa el valor
  - Falla: `~/` trunca hacia cero pero `%` en Dart siempre devuelve positivo, y las dos mitades se concatenan como si fueran el signo y la magnitud del mismo numero. Verificado ejecutandolo: -199 centavos exporta '-1,01' (deberia ser -1,99), -99 exporta '0,01' (deberia ser -0,99, con el signo perdido del todo) y -1 exporta '0,99'. Solo coincide por casualidad cuando el resto es 0 o 50. Hoy nada escribe montos negativos (`addManualExpense` y `GroupRepository.addExpense` rechazan `<= 0`), asi que hace falta un documento con `amountCents` negativo: uno viejo con `amount` double negativo, o uno hecho a mano. Pero se vuelve alcanzable en cuanto existan devoluciones o el 'asiento de ajuste' que docs/grupos-y-division.md propone para editar un gasto ya liquidado, y la falla es silenciosa: la planilla que la persona se lleva como respaldo de sus datos tiene un numero que no es el que estaba guardado.
  - Propuesta: Formatear sobre el valor absoluto y anteponer el signo: `final neg = cents < 0; final abs = cents.abs(); final entero = abs ~/ 100; final resto = (abs % 100).toString().padLeft(2, '0'); return '${neg ? '-' : ''}$entero${decimalComma ? ',' : '.'}$resto';`. Ademas usar `Money.minorUnits` en vez del 100 hardcodeado.

### `lib/services/ai/ai_service.dart`

- **[baja]:77** El prompt de clasificacion por IA manda los precios en centavos con simbolo de pesos: 100x el valor real
  - Falla: Un ticket con 'Coca Cola 1.5L' a $120,00 (12000 centavos) se le manda al modelo como '- Coca Cola 1.5L: $12000'. El modelo recibe precios cien veces mas altos que los reales y clasifica en base a eso: un ticket de almacen de $2.500 se le presenta como si fueran $250.000, lo que empuja hacia categorias equivocadas. Es el camino secundario (solo corre si MerchantClassifier no resolvio y enableAiClassification esta prendido), pero cuando corre, corre mal.
  - Propuesta: Usar el mismo formateo que ya usa generateMonthlyInsights:
  final itemsList = items.map((i) => '- ${i.name}: ${Money.format(i.totalPriceCents)}').join('\n');
- **[baja]:77** El prompt de clasificacion le manda al modelo los centavos con signo de pesos adelante
  - Falla: `i.totalPriceCents` son centavos y se interpola detras de un `\$` como si fueran pesos. Un item de $150,00 (15000 centavos) llega al modelo como `- Coca Cola 1.5L: $15000`. Todos los precios del ticket le llegan 100x inflados, que es justo la senal que un clasificador usa para distinguir 'supermercado' de 'restaurante' o 'compras'. Es la unica interpolacion de plata del archivo que no pasa por `Money.format` (las dos de `generateMonthlyInsights`, lineas 111 y 117, si lo usan). Impacto acotado hoy porque el clasificador local corre primero y la IA apunta a localhost, pero queda como bomba para cuando llegue el proxy.
  - Propuesta: `final itemsList = items.map((i) => '- ${i.name}: ${Money.format(i.totalPriceCents)}').join('\n');` -- `Money` ya esta importado en el archivo.
